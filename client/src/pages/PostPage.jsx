// here fetching from post from the server(get endpoints), which is geetting the posts from the db
export default function PostPage(){
    //we need useEffect to fetch the data from the server
    // useEffect()
    //we need a function to fetchData({
    //fetch("url")
    //parse the data into json
    //console.log your data, so you can see what it looks like, might have to wrangle data, depending on what data looks like
    // })
    return(
        <>
        <h1>My Posts</h1>
        {/* render my post in here */}
        {/* use some conditional rendering to show the posts */}
        </>
    )
}